package minesweeper.model;

public class MinedCell extends Cell {    
    
    public MinedCell(int row, int column, int width, int height, CellState state) {
        super(row, column, width, height, state);
    }
    
}